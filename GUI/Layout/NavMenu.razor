@using MudBlazor;   

<MudNavMenu Class="NavMenu">
    <MudNavGroup Title="Menu" Expanded="true" class="nav-main-menu-item">
        <MudNavLink Href="/client">Klienci</MudNavLink>
        <MudNavLink Href="/warehouse">Magazyn</MudNavLink>
        <MudNavLink Href="/order">Zamówienia</MudNavLink>
    </MudNavGroup>
</MudNavMenu>


@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
